1. Дан код:

var a = 1, b = 1, c, d;
c = ++a; alert(c);           // 2 - Сначала инкремент к переменной, а потом запись ее в с
d = b++; alert(d);           // 1 - Сначало запись переменной b в d, а потом инкремент
c = (2+ ++a); alert(c);      // 5 - т.к. а у нас теперь имеен значение 2, то сначала применится инкремент, а потом +2
d = (2+ b++); alert(d);      // 4 - b у нас тоже имеет значение 2, но сначала поизайдет сложение с 2 и записть результата в с, а потом инкремент.
alert(a);                    // 3 - операция инкремента прошла 2 раза
alert(b);                    // 3 - операция инкремента прошла 2 раза

Почему код даёт именно такие результаты?


2. Чему будет равен x в примере ниже?

var a = 2;
var x = 1 + (a *= 2); - переменной а присваивается значение с умножением на 2 и прибавляется 1.
x = 5